# SynchroniSDKPython 测试计划

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 被测对象 | `sensor-sdk`（OYMotion / Synchroni BLE 传感器 Python SDK） |
| 目标版本 | **0.8.0**（接口以 0.8.0 README 为准；Python 3.10~3.14） |
| 测试范围 | SensorController、SensorProfile、数据流、参数、多设备同步、Bin 录制/回放/解析、日志、电量 |
| 文档状态 | 计划（待评审） |
| 前置输入 | [README.md](../README.md)、[examples](../examples)（console.py / async_console.py / multi_start_test.py / SynchroniSDKPython_DemoNewMulti*.py） |

> 说明：本计划为“测试计划文档”，仅定义测试策略与用例设计，不包含自动化脚本实现。用例字段遵循软硬件集成测试模板（正/逆/边界/组合/状态迁移/异常恢复）。

---

## 2. 测试范围与对象

### 2.1 被测 SDK 模块

| 模块 | 公开接口（被测点） |
|------|--------------------|
| 控制器 `SensorController` | `isEnable`、`onEnableCallback`、`hasDeviceFoundCallback`、`onDeviceFoundCallback`、`startScan(period_in_ms)`、`scan(period_in_ms)`、`asyncScan`、`stopScan()`、`isScanning`、`requireSensor(device)`、`getSensor(mac)`、`getConnectedSensors()`、`getConnectedDevices()`、`getBLEBackendName()`、`checkSetupDongle()`、`multiStartDataNotification(...)`、`asyncMultiStartDataNotification(...)`、`multiStopDataNotification(...)`、`asyncMultiStopDataNotification(...)`、`getBinFileInfo(path)`、`replayBinFile(...)`、`pauseBinReplay/resumeBinReplay/stopBinReplay`、`parseBinToCsv(...)`、`setDebugEnabled(bool)`、`setLogPath(...)`、`log(msg, level)`、`getVersion()`、`terminate()` |
| 设备 `SensorProfile` | 回调 `onStateChanged/onErrorCallback/onPowerChanged/onDataCallback/onAutoReconnect/onDeviceInfoUpdate`、`autoReconnect`、`isReady`、`connect()/asyncConnect`、`disconnect()/asyncDisconnect`、`deviceState`、`BLEDevice`、`getDeviceInfo()`、`init(...)/asyncInit`、`hasInited`、`startDataNotification()/asyncStartDataNotification`、`stopDataNotification()/asyncStopDataNotification`、`isDataTransfering`、`getBatteryLevel()/asyncGetBatteryLevel`、`setParam/asyncSetParam`、`getParam/asyncGetParam`、`log(msg, level)` |
| 枚举/数据结构 | `DeviceStateEx`（Disconnected/Connecting/Connected/Ready/Disconnecting/Invalid）、`DataType`（16 种，见 2.3）、`DeviceInfo`、`SensorData`、`Sample`、`BLEDevice`（新字段/接口见 2.4） |

### 2.2 支持的设备类型清单（多设备）

SDK 支持多设备并存，且同一次扫描/连接/起流可操作多台。由于本仓库未提供具体设备型号清单，以下按**可观测的区分维度**列出“设备类型”，每类都需要纳入计划：

#### A. 按连接/协议/命名区分

| 设备类型 | 识别特征 | 备注 |
|----------|----------|------|
| OYM 命名设备 | 广播名前缀 `OY`（如 `OYWW1000`） | 传统 OYMotion 设备 |
| OB 命名设备 | 广播名前缀 `OB`（如 `OB6000`） | 多设备同步测试的典型目标设备 |
| Sync 命名设备 | 广播名前缀 `Sync` | console.py 中与 `OB` 并列过滤 |
| RFSTAR 芯片设备 | bin 配置 `chip_type=RFSTAR`；起流/停流走 `set_subscription` 命令 | 与 OYM 的 CCCD 写路径不同 |
| NeuCir 设备 | 支持 `NEUCIR_SET_MODE`/`NEUCIR_APP_CONTROL` 参数 | 远程控制类设备 |

#### B. 按数据模态/能力区分（由 `DeviceInfo.*ChannelCount/*SampleRate` 决定）

| 设备类型 | 关键能力字段 / 数据流 | 备注 |
|----------|------------------------|------|
| 传统 EMG 设备 | `Emg*`，`NTF_EMG` | `NTF_GEST` 与 `NTF_EMG` 互斥 |
| 新 EMG 设备 | `Emg*`，`NTF_IMU`（聚合 acc/gyro/euler/quat）+ `NTF_GEST` | `lostPackageCount` 非 0 仅新 EMG 设备 |
| EEG 设备 | `Eeg*`，`NTF_EEG` | 支持 `EEG_SAMPLE_RATE` |
| ECG 设备 | `Ecg*`，`NTF_ECG` | 与 EEG 采样率绑定 |
| 呼吸 BRTH 设备 | `Brth*`，`NTF_BRTH` | 单位 uV |
| PPG/SpO2 设备 | `Ppg*`/`Spo2*`，`NTF_PPG` + `NTF_SPO2` | 常含 EEG fp1/fp2 |
| IMU 设备 | `Acc*/Gyro*/Euler*/Quat*`，`NTF_ACC/GYRO/EULER/QUAT` | 传统四路独立流 |
| 磁角度 MagAngle 设备 | `MagAngle*`，`NTF_MAG_ANGLE_DATA` | — |
| 阻抗 Impedance 设备 | `Impe*`，`NTF_IMPEDANCE` / `NTF_IMPEDANCE_EXT` | — |

> 上述两类维度是**正交**的：同一物理设备可能同时属于“OB 命名” + “新 EMG 设备” + “IMU 设备”。计划中“连接/生命周期/状态机”用例按 A 类逐类型覆盖，“数据流/参数”用例按 B 类逐模态覆盖。

### 2.3 数据流类型清单（DataType）

`NTF_ACC`、`NTF_GYRO`、`NTF_EULER_DATA`、`NTF_QUATERNION`、`NTF_GEST`、`NTF_EMG`、`NTF_MAG_ANGLE_DATA`、`NTF_EEG`、`NTF_ECG`、`NTF_IMPEDANCE`、`NTF_IMU`、`NTF_ADS`、`NTF_BRTH`、`NTF_IMPEDANCE_EXT`、`NTF_SPO2`、`NTF_PPG`。

### 2.4 数据结构新增字段/接口（0.8.0）

**`DeviceInfo`（`getDeviceInfo()` 返回）新增字段**

| 字段 | 含义 |
|------|------|
| `EmgMaxSampleRate` / `EegMaxSampleRate` / `EcgMaxSampleRate` | 设备能力上报的最大采样率（0 = 未上报） |
| `ImuChannelCount` / `ImuSampleRate` | 聚合 `NTF_IMU` 流（仅新 EMG 设备；0 = 无聚合流，改用四路独立 ACC/GYRO/EULER/QUAT） |
| `ConnectionIntervalMs` / `PeripheralLatency` / `SupervisionTimeoutMs` | BLE 链路协商参数（仅 bumble dongle 后端；0/-1/0 = 未知） |

**`SensorData` 新增接口**

- 元数据：`getDeviceMac()` / `getChannelMask()` / `isDataValid()`（Python 恒为 True）。
- 整批：`channelSamples` / `startSampleIndex`（只读）、`clone()`（深拷贝）。
- 单点访问器（`ci`=通道索引，`si`=样本索引）：`getChannelSample(ci,si)` 返回 `Sample`；`getData(ci,si)` / `getRawData(ci,si)` / `getImpedance(ci,si)` / `getSaturation(ci,si)` / `getSampleIndex(ci,si)` / `getTimeStampInMs(ci,si)` / `isLost(ci,si)`。

**`Sample` 新增只读字段**：`rawData` / `impedance` / `saturation` / `channelIndex` / `timeStampInMs`（原有 `data` / `sampleIndex` / `isLost`）。

**回调交付形式**：`onDataCallback` 每次交付 `list[SensorData]`（0.5.0 起为 list，非单个对象）。

---

## 3. 测试环境与前置条件

### 3.1 硬件环境

| 项目 | 要求 |
|------|------|
| 被测设备 | 上述 2.2 列出的各类型设备各 ≥1 台（若同型号有多台则 ≥2 台用于多设备同步） |
| 蓝牙接口 | 原生 BLE（bleak 后端）或 USB BLE dongle（bumble 后端，如 `10d7:b012`/`33fa:0012`） |
| 测试主机 | Windows 10/11（本环境）、Linux、macOS（跨平台用例） |
| 仪器（人工用例） | 见 §5.9 |

### 3.2 软件环境

- Python 3.10 ~ 3.14（sensor-sdk 0.8.0 提供 `cp310`~`cp314` 编译 wheel；建议 3.12+）
- `pip install --upgrade sensor-sdk`（0.8.0；依赖 `bleak>=3.0.2`、`bumble==0.0.233`、`libusb1`、`numpy` 等）
- 示例依赖：`scipy`、`matplotlib`、`PyQt5`（可选，用于 Qt demo）

### 3.3 通用前置条件

- 蓝牙已开启，或 dongle 已正确绑定驱动（`checkSetupDongle()` 返回 `OK`）。
- 设备已上电、在扫描范围内、广播名符合预期前缀。
- 每次测试结束调用 `SensorControllerInstance.terminate()`；异常退出（Ctrl+C）需经 `terminate()` 释放资源。
- 测试间运动/数据类用例间隔 ≥100ms，避免指令错乱。

---

## 4. 测试策略

### 4.1 测试维度（12 维度）

| 维度 | 覆盖重点 | 优先级 | 执行方式 |
|------|----------|--------|----------|
| 功能 | 全部公开 API，按正/逆/边界/组合/状态迁移设计 | P0 | 自动 |
| 性能 | 往返延迟、吞吐量、长时间稳定性、冷启动 | P1 | 自动 |
| 安全性 | 参数边界、异常恢复、急停/断连保护 | P0 | 自动+人工 |
| 健壮性 | 并发、断连重连、不完整帧容错、资源泄漏 | P1 | 自动 |
| 通信专项 | dongle 后端选择、MTU、帧分片、端口边界 | P0 | 自动 |
| 兼容性 | 跨平台、Python 版本、不同设备类型 | P2 | 自动 |
| 系统维护 | `terminate()`、日志开关、bin 导出、`checkSetupDongle` | P1-P2 | 自动 |
| 不适用 | 不可逆/需密钥操作 | — | 人工 |
| 信号完整性 | BLE 波形、时延、同步对齐（物理波形归硬件团队；同步对齐归 SDK 软件用例） | P1 | 人工+仪器 |
| 电源 | 设备供电、上掉电（归硬件团队） | P1 | 人工 |
| 环境可靠性 | 温湿度运行（归硬件团队） | P2 | 人工 |
| 老化 | 连续运行 24h/72h（硬件寿命归硬件团队；SDK 长稳见 `DATA-PERF-002`） | P2 | 半自动 |

> 备注：「信号完整性（物理波形）」「电源」「环境可靠性」「老化（硬件寿命）」等维度**超出 SDK 软件测试范围**，仅作记录、归口硬件/产品可靠性团队。其中与 SDK 相关的软件行为已并入对应用例：断电/上掉电重连 → `DEV-SM-012` / `HW-MAN-003`；长稳 → `DATA-PERF-002`；多设备同步对齐/时延 → `MULTI-FUNC-004/008/009`。

### 4.2 用例设计方法（强制）

每个数值参数用**边界值 6 点法**；非法输入用**等价类无效值 + 错误推测**；多参数用**判定表**；设备生命周期用**状态迁移**；断连/断电用**异常恢复**。涉及数值型参数（如 `init(packageSampleCount, powerRefreshInterval)`、`startScan(period_in_ms)`、`multiStartDataNotification(maxDelayDispersionMs, maxAttempts, timeout)`、`EEG_SAMPLE_RATE`）必须覆盖 6 点边界。

### 4.3 执行方式划分

- **auto**：无需人工/仪器，脚本可判定。
- **semi-auto**：需人工触发物理动作（如拔 dongle、移动设备、断电），脚本采集与判定。
- **manual**：需仪器或不可逆操作，人工执行并记录。

---

## 5. 测试用例设计

### 5.1 SensorController（控制器）

| case_id | 标题 | 优先级 | 类型 | 执行方式 |
|---------|------|--------|------|----------|
| CTRL-FUNC-001 | 蓝牙未开启时 `isEnable==False`，`startScan` 应被拒绝/提示 | P0 | 逆向 | auto |
| CTRL-FUNC-002 | 蓝牙开启时 `isEnable==True`，`onEnableCallback` 随开关状态触发 | P0 | 正向 | auto |
| CTRL-FUNC-003 | `startScan(period_in_ms)` 成功返回 True，`isScanning` 翻转 | P0 | 正向 | auto |
| CTRL-FUNC-004 | `scan(period_in_ms)` / `asyncScan` 单次扫描返回 `list[BLEDevice]`，含 `Name/Address/RSSI` | P0 | 正向 | auto |
| CTRL-FUNC-005 | `onDeviceFoundCallback` 每 `period_in_ms` 周期触发一次，返回未连接设备 | P0 | 正向 | auto |
| CTRL-FUNC-006 | `stopScan()` 后 `isScanning==False`，回调停止 | P0 | 正向 | auto |
| CTRL-FUNC-007 | `requireSensor(有效 BLEDevice)` 返回 SensorProfile；无效/空返回 None | P0 | 正/逆 | auto |
| CTRL-FUNC-008 | `getSensor(已创建 mac)` 返回 profile；未创建返回 None | P1 | 正/逆 | auto |
| CTRL-FUNC-009 | `getConnectedSensors()` / `getConnectedDevices()` 返回当前已连接集合 | P0 | 正向 | auto |
| CTRL-FUNC-010 | `getBLEBackendName()` 返回 `bumble` 或 `bleak`，与 `SENSOR_SDK_BLE_BACKEND` 一致 | P1 | 正向 | auto |
| CTRL-FUNC-011 | `checkSetupDongle()` 在无 dongle 时执行引导并返回 `OK: N` 或 `Error: ...` | P1 | 正向 | semi-auto |
| CTRL-FUNC-012 | `getVersion()` 返回非空版本号 | P2 | 正向 | auto |
| CTRL-FUNC-013 | `terminate()` 正常释放，重复调用不抛异常 | P0 | 异常恢复 | auto |
| CTRL-BND-001 | `startScan(period_in_ms)` 6 点边界：`0`、`1`、`MAX-1`、`MAX`、`-1`、`MAX+1` | P1 | 边界 | auto |
| CTRL-ROB-001 | 扫描期间重复 `startScan` / 反复 `stopScan`，无死锁无异常 | P1 | 异常恢复 | auto |

### 5.2 设备连接 / 状态机（按 §2.2-A 每类型逐项执行）

> 下列用例在 OYM / OB / Sync / RFSTAR / NeuCir 每类设备上各执行一遍。

| case_id | 标题 | 优先级 | 类型 | 执行方式 |
|---------|------|--------|------|----------|
| DEV-SM-001 | `connect()` 成功返回 True，状态迁移 `Disconnected→Connecting→Connected→Ready` | P0 | 状态迁移 | auto |
| DEV-SM-002 | `connect()` 失败返回 False，`onErrorCallback` 收到原因 | P0 | 异常恢复 | auto |
| DEV-SM-003 | 扫描到非目标前缀设备时 `requireSensor` 后不误连接 | P1 | 逆向 | auto |
| DEV-SM-004 | `disconnect()` 返回 True，状态迁移 `Ready→Disconnecting→Disconnected`，`onStateChanged` 触发 | P0 | 状态迁移 | auto |
| DEV-SM-005 | 起流中调用 `disconnect()` 自动先 `stopDataNotification` 再断开 | P0 | 正向 | auto |
| DEV-SM-006 | `deviceState` 各枚举值语义正确（Ready 前不发送命令） | P0 | 状态迁移 | auto |
| DEV-SM-007 | `BLEDevice` 的 Name/Address/RSSI 与扫描结果一致 | P1 | 正向 | auto |
| DEV-SM-008 | `autoReconnect=True`（默认）异常断链后自动 `connect→init→恢复 setParam→startDataNotification` | P0 | 异常恢复 | semi-auto |
| DEV-SM-009 | `autoReconnect=False` 异常断链后不自动重连 | P1 | 异常恢复 | semi-auto |
| DEV-SM-010 | `onAutoReconnect(restore=True)` 自定义恢复路径生效，返回 True 跳过默认流程 | P1 | 异常恢复 | semi-auto |
| DEV-SM-011 | 多台设备同时 `connect()`，互不干扰（并发） | P0 | 组合 | auto |
| DEV-SM-012 | 目标设备突然断电/超出范围：`onStateChanged(Disconnected)` 与 `onErrorCallback` 触发 | P0 | 异常恢复 | semi-auto |
| DEV-SM-013 | `isReady` 属性等于 `deviceState == DeviceStateEx.Ready`（Ready 后 True，断开后 False） | P1 | 正向 | auto |
| DEV-SM-014 | `onDeviceInfoUpdate` 在 init 后链路参数更新（bumble 后端）或 `EEG_SAMPLE_RATE` 变更时触发，且返回的 `DeviceInfo` 已就地更新 | P1 | 正向 | auto |

### 5.3 数据流（按 §2.2-B 每模态逐项执行）

| case_id | 标题 | 优先级 | 类型 | 执行方式 |
|---------|------|--------|------|----------|
| DATA-FUNC-001 | `init(packageSampleCount, powerRefreshInterval)` 成功返回 True，`hasInited==True` | P0 | 正向 | auto |
| DATA-FUNC-002 | 未 `connect`/`Ready` 前调用 `init`/`startDataNotification` 应失败 | P0 | 逆向 | auto |
| DATA-FUNC-003 | `startDataNotification()` 成功后 `isDataTransfering==True`，`onDataCallback` 按批触发 | P0 | 正向 | auto |
| DATA-FUNC-004 | `stopDataNotification()` 成功后 `isDataTransfering==False`，回调停止 | P0 | 正向 | auto |
| DATA-FUNC-005 | 每个 `DataType` 流（§2.3）均能收到对应 `SensorData`，`getDataType()` 匹配 | P0 | 正向 | auto |
| DATA-FUNC-006 | `SensorData` 元数据（`getDeviceMac/getDataType/getSampleRate/getChannelCount/getChannelMask/getSampleCount/getLostPackageCount/getStartTimeStamp/getDelay/isDataValid`）数值合法 | P0 | 正向 | auto |
| DATA-FUNC-007 | `channelSamples`/`getChannelSample(ci,si)`/`getData(ci,si)`/`getRawData(ci,si)`/`getImpedance(ci,si)`/`getSaturation(ci,si)`/`getSampleIndex(ci,si)`/`getTimeStampInMs(ci,si)`/`isLost(ci,si)` 读取一致，`sampleIndex` 单调递增 | P0 | 正向 | auto |
| DATA-FUNC-008 | `sample.isLost` 置位样本正确标记，`getLostPackageCount` 与丢包数一致 | P1 | 异常恢复 | auto |
| DATA-FUNC-009 | 每批样本数 ≈ `packageSampleCount`（允许边界批不足） | P1 | 正向 | auto |
| DATA-FUNC-010 | 新 EMG 设备 `NTF_IMU` 聚合批通道布局（acc 0-2/gyro 3-5/euler 6-8/quat 9-12）正确 | P1 | 正向 | auto |
| DATA-FUNC-011 | 传统 EMG 设备 `NTF_GEST` 与 `NTF_EMG` 互斥（同时 ON 只生效其一） | P1 | 逆向 | auto |
| DATA-FUNC-012 | `SensorData.clone()` 返回深拷贝，修改副本不影响原对象 | P1 | 正向 | auto |
| DATA-FUNC-013 | `Sample` 新增字段（`rawData/impedance/saturation/channelIndex/timeStampInMs`）与单点访问器（`getData/getImpedance/getSaturation/getTimeStampInMs`）一致 | P1 | 正向 | auto |
| DATA-BND-001 | `init(packageSampleCount)` 6 点边界：`1`、`2`、`MAX-1`、`MAX`、`0`、`MAX+1` | P1 | 边界 | auto |
| DATA-BND-002 | `init(_, powerRefreshInterval)` 6 点边界（0、1、上限邻域、上限、负、超上限） | P1 | 边界 | auto |
| DATA-PERF-001 | 实测采样率与标称采样率偏差 ≤ 容差（各模态） | P1 | 性能 | auto |
| DATA-PERF-002 | 连续起流 ≥ 30min 无内存/句柄泄漏、无掉速 | P1 | 性能 | semi-auto |

### 5.4 setParam / getParam

| case_id | 标题 | 优先级 | 类型 | 执行方式 |
|---------|------|--------|------|----------|
| PARAM-FUNC-001 | 每个 `NTF_*` 键 `setParam("ON"/"OFF")` 返回 `OK`，`getParam("NTF")` 状态一致 | P0 | 正向 | auto |
| PARAM-FUNC-002 | 每个 `FILTER_*` 键 `setParam("ON"/"OFF")` 返回 `OK`，`getParam("FILTER")` 一致 | P0 | 正向 | auto |
| PARAM-FUNC-003 | `setParam("NTF_IMU")` 联动更新四个 `NTF_GFORCE_*`；反之亦然 | P1 | 组合 | auto |
| PARAM-FUNC-004 | `setParam("EEG_SAMPLE_RATE")` 取 `EEG_SAMPLE_RATE_LIST` 中值成功；EEG/ECG 同写 | P0 | 正向 | auto |
| PARAM-FUNC-005 | `setParam("EEG_SAMPLE_RATE")` 传不支持值返回 `Error: unsupported sample rate ...` | P0 | 逆向 | auto |
| PARAM-FUNC-006 | `getParam("EEG_SAMPLE_RATE")` 返回当前采样率；`getParam("EEG_SAMPLE_RATE_LIST")` 返回管道分隔可选列表；不支持返回 `Error` | P1 | 正向 | auto |
| PARAM-FUNC-007 | 起流中修改 `NTF_*`/`FILTER_*`/`EEG_SAMPLE_RATE` 自动停流重起并生效 | P0 | 状态迁移 | auto |
| PARAM-FUNC-008 | `setParam` 传未知 key 返回以 `Error` 开头 | P1 | 逆向 | auto |
| PARAM-FUNC-009 | `NEUCIR_SET_MODE`/`NEUCIR_APP_CONTROL`（仅 NeuCir 设备）生效 | P1 | 正向 | auto |
| PARAM-FUNC-010 | `DEBUG_BLE_DATA_PATH`/`DEBUG_LOG_PATH` 设置/读取/关闭 | P1 | 正向 | auto |
| PARAM-BND-001 | `setParam` 各 key 的非法 value（空串、`ON/OFF` 以外、大小写、None） | P1 | 边界 | auto |

### 5.5 多设备同步

> 签名（0.8.0）：`multiStartDataNotification(sensors, timeout=30.0, maxDelayDispersionMs=5, maxAttempts=3)`；`multiStopDataNotification(sensors, timeout=10.0)`。

| case_id | 标题 | 优先级 | 类型 | 执行方式 |
|---------|------|--------|------|----------|
| MULTI-FUNC-001 | `multiStartDataNotification([s1,s2])` 返回 `{mac: bool}`，两台同时起流 | P0 | 组合 | auto |
| MULTI-FUNC-002 | `multiStopDataNotification([s1,s2])` 返回 `{mac: bool}`，两台同时停流 | P0 | 组合 | auto |
| MULTI-FUNC-003 | 某台未 `Ready`/未 `init` 时，其余设备不受影响仍可起流 | P0 | 逆向 | auto |
| MULTI-FUNC-004 | 首包时差 ≤ `maxDelayDispersionMs`（默认 5ms）通过；超差自动停止并重试至 `maxAttempts` | P0 | 边界 | auto |
| MULTI-FUNC-005 | `maxDelayDispersionMs=-1` 跳过时差校验，仅要求首包到达 | P1 | 边界 | auto |
| MULTI-FUNC-006 | 混合型号设备用放宽参数（`timeout=60,maxDelayDispersionMs=-1,maxAttempts=5`） | P1 | 组合 | auto |
| MULTI-FUNC-007 | 已起流设备再次 `multiStart`（restart 语义：先停后与空闲设备同刻起流） | P1 | 状态迁移 | auto |
| MULTI-FUNC-008 | bumble 后端 `SyncWriteGate` 对齐下发；bleak 后端并发下发（两后端各测） | P0 | 通信专项 | auto |
| MULTI-FUNC-009 | 跨设备 `startTimeStamp` 散布与 `getDelay()` 符合预期 | P1 | 性能 | auto |

### 5.6 Bin 录制 / 回放 / 解析

| case_id | 标题 | 优先级 | 类型 | 执行方式 |
|---------|------|--------|------|----------|
| BIN-FUNC-001 | 成功连接后自动生成 `.bin`（`{DeviceName}_{MAC}_{时间戳}.bin`） | P0 | 正向 | auto |
| BIN-FUNC-002 | `getBinFileInfo(有效 bin)` 返回含 `device_mac/device_name/chip_type/replay_duration` 的 dict | P0 | 正向 | auto |
| BIN-FUNC-003 | `getBinFileInfo(不存在/无 config)` 返回 None | P0 | 逆向 | auto |
| BIN-FUNC-004 | `replayBinFile(path, sensor, realtime=True)` 按原始节奏回放，`onDataCallback` 收到数据 | P0 | 正向 | auto |
| BIN-FUNC-005 | `replayBinFile(realtime=False)` 全速回放，回放数据批数与实收一致 | P1 | 正向 | auto |
| BIN-FUNC-006 | `pauseBinReplay`/`resumeBinReplay`/`stopBinReplay` 返回 `OK`，暂停/恢复/中止生效 | P1 | 状态迁移 | auto |
| BIN-FUNC-007 | 目标 sensor 正在实时起流时拒绝回放 | P1 | 逆向 | auto |
| BIN-FUNC-008 | `parseBinToCsv` 生成 CSV，含 `raw/cmd_send/cmd_recv/event/parsed` 行，表头符合约定 | P1 | 正向 | auto |
| BIN-FUNC-009 | 回放还原的 `startTimeStamp/delay` 与实收值一致（multi_start_test.py 场景） | P1 | 正向 | auto |
| BIN-BND-001 | 默认（未设 `DEBUG_BLE_DATA_PATH`）时不持久落盘，临时文件断开后删除 | P1 | 逆向 | auto |
| BIN-ROB-001 | 磁盘 < 100MB 或写盘错误时跳过/停止录制且不影响实时流 | P2 | 异常恢复 | semi-auto |

### 5.7 电量 / 日志 / 调试

| case_id | 标题 | 优先级 | 类型 | 执行方式 |
|---------|------|--------|------|----------|
| BATT-FUNC-001 | `getBatteryLevel()` 返回 0~100；无有效读数返回 -1 | P0 | 正向 | auto |
| BATT-FUNC-002 | `onPowerChanged` 上报 0~100，±2% 滞回（1% 抖动被过滤），不报 -1 | P1 | 正向 | auto |
| BATT-FUNC-003 | `powerRefreshInterval` 决定 `onPowerChanged` 周期 | P1 | 性能 | auto |
| BATT-FUNC-004 | `startDataNotification` 起流期间 `onPowerChanged` 持续回调（不因起流而停止） | P1 | 正向 | auto |
| LOG-FUNC-001 | `setDebugEnabled(True)` 创建 controller log；`False` 关闭 | P1 | 正向 | auto |
| LOG-FUNC-002 | `setLogPath(True, dir)` 创建目录；指向已存在文件被拒绝 | P1 | 正/逆 | auto |
| LOG-FUNC-003 | `DEBUG_LOG_PATH=True` 生成每设备 profile log；`getParam("DEBUG_LOG_PATH")` 返回路径 | P1 | 正向 | auto |
| LOG-FUNC-004 | 早期 scan/connect 日志不丢失（buffer 回放） | P2 | 正向 | auto |
| LOG-FUNC-005 | `SensorController.log(msg, level)` / `SensorProfile.log(msg, level)` 写入应用日志（level `D/I/W/E`），带 `[App]` 标记；profile 未启用时回退 controller log | P1 | 正向 | auto |

### 5.8 边界值分析汇总（6 点法）

| 参数 | 范围 | B1~B4（合法） | B5/B6（非法） | 补充 |
|------|------|----------------|---------------|------|
| `startScan(period_in_ms)` | >0 | min、min+1、max-1、max | min-1(0/负)、max+1 | None/字符串 |
| `init(packageSampleCount)` | ≥1 | 1、2、N-1、N | 0、N+1 | None/字符串 |
| `init(powerRefreshInterval)` | ≥0 | 0、1、N-1、N | -1、N+1 | None |
| `multiStartDataNotification(maxDelayDispersionMs)` | ≥0 或 -1 跳过 | 0、1、N-1、N、-1 | — | 非整型 |
| `multiStartDataNotification(maxAttempts)` | ≥1 | 1、2、N-1、N | 0、N+1 | — |
| `multiStartDataNotification(timeout)` | >0 | 0.1、1、N-1、N | 0、负 | 默认 30.0 |
| `EEG_SAMPLE_RATE` | 列表内值 | 每个列表值 | 列表外值 | 空串 |
| `getBatteryLevel()` 返回 | 0~100 或 -1 | 0、1、99、100 | — | -1 表示无读数 |

### 5.9 人工硬件用例（manual）

> 说明：`HW-MAN-003`（供电电压/时序）、`HW-MAN-004`（老化寿命）、`HW-MAN-005`（高低温）偏硬件可靠性，**超出 SDK 软件测试范围**、归口硬件团队；其中 SDK 软件响应部分（断电/上掉电重连）由 `DEV-SM-012` 覆盖。`HW-MAN-001/002`（BLE 链路稳定性、dongle 驱动绑定）与 SDK 通信直接相关，保留在 SDK 测试内。

| case_id | 标题 | 优先级 | 类型 | 执行方式 |
|---------|------|--------|------|----------|
| HW-MAN-001 | BLE 通信链路 RSSI/连接稳定性人工验证（各设备类型） | P1 | 正向 | manual |
| HW-MAN-002 | USB BLE dongle 驱动绑定与 `checkSetupDongle()` 引导流程 | P1 | 正向 | manual |
| HW-MAN-003 | 设备供电电压/上掉电时序与 SDK 重连恢复 | P1 | 异常恢复 | manual |
| HW-MAN-004 | 24h 连续起流老化稳定性 | P2 | 老化 | semi-auto |
| HW-MAN-005 | 高低温环境运行（各设备类型） | P2 | 环境可靠性 | manual |

> 完整人工用例（仪器清单、环境、接线、逐步操作、判定标准、后置处理）在进入执行阶段时按 §5.9 模板单独展开为 `test_manual_hardware.md`。

---

## 6. 每设备类型测试矩阵（汇总）

| 设备类型 | 连接/状态机 | 数据流 | 参数 | 多设备同步 | 人工 |
|----------|:-----------:|:------:|:----:|:----------:|:----:|
| OYM 命名设备 | ✓ | ✓ | ✓ | ✓ | ✓ |
| OB 命名设备（如 OB6000） | ✓ | ✓ | ✓ | ✓ | ✓ |
| Sync 命名设备 | ✓ | ✓ | ✓ | ✓ | ✓ |
| RFSTAR 芯片设备 | ✓ | ✓ | ✓ | ✓ | ✓ |
| NeuCir 设备 | ✓ | ✓ | ✓（NEUCIR_*） | ✓ | ✓ |
| 传统 EMG 设备 | ✓ | ✓（EMG/GEST 互斥） | ✓ | — | ✓ |
| 新 EMG 设备 | ✓ | ✓（NTF_IMU 聚合） | ✓ | — | ✓ |
| EEG 设备 | ✓ | ✓（EEG） | ✓（采样率） | — | ✓ |
| ECG 设备 | ✓ | ✓（ECG） | ✓（采样率绑定） | — | ✓ |
| BRTH 设备 | ✓ | ✓（BRTH） | ✓ | — | ✓ |
| PPG/SpO2 设备 | ✓ | ✓（PPG/SpO2/EEG） | ✓ | — | ✓ |
| IMU 设备 | ✓ | ✓（ACC/GYRO/Euler/Quat） | ✓ | — | ✓ |
| MagAngle 设备 | ✓ | ✓（MAG_ANGLE） | ✓ | — | ✓ |
| Impedance 设备 | ✓ | ✓（IMPEDANCE/EXT） | ✓ | — | ✓ |

---

## 7. 判定标准与通过率目标

- 单用例通过标准：`expected` 全部命中，且无未捕获异常、无内存/句柄泄漏。
- P0 用例：**100% 通过**；P1：≥95%；P2：≥90%。
- 所有数值参数必须通过 6 点边界值分析；所有设备类型必须覆盖连接/数据流用例。
- 失败用例须保留 SDK 日志（controller/profile log）与 `.bin`，用于复现。

---

## 8. 风险与安全注意事项

1. **连接/断电/断连用例**：执行前确认目标设备无人员接触电极/运动部件；异常注入（拔 dongle、断电）仅允许对测试专用设备执行。
2. **多设备同步**：多台设备同时起流时，确保设备固定、运动范围无人员。
3. **破坏性/不可逆操作**（如复位、改 ID、标定等）**不纳入自动化**，归入人工并按文档单独评估。
4. **`terminate()` 必须调用**：所有脚本（含异常退出路径）须在 finally 中调用，避免 BLE 子进程/句柄泄漏。
5. **测试间留足间隔**：起流/停流/连接类操作之间 ≥100ms。
6. **配置项外置**：串口/dongle/设备前缀/MAC 等写入独立 `config.py`，不硬编码。
7. **后端兼容**：`bumble`（dongle）与 `bleak`（原生）两后端各跑一遍通信/同步用例。
8. **回调线程安全**：`onDataCallback` 单线程顺序交付，`onStateChanged` 同步调用，回调内保持短小或线程安全。

---

## 9. 交付物（进入执行阶段后）

- 自动化用例：`test_ctrl.py`、`test_device_state.py`、`test_data_stream.py`、`test_param.py`、`test_multi_sync.py`、`test_bin_replay.py`、`test_battery_log.py`、`test_boundary.py`。
- 人工用例：`test_manual_hardware.md`。
- 配置：`config.py`；主入口：`run_all.py`（`--skip-manual` 跳过人工）。
- 报告与截图：`reports/`、`reports/screenshots/`。
