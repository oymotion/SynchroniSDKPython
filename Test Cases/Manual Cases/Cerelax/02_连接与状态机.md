# Cerelax 连接与状态机用例（DEV-SM-*）

> 下列用例均针对 Cerelax 单台执行。

### DEV-SM-001 connect 成功，状态迁移 Disconnected→Connecting→Connected→Ready
- **优先级**：P0
- **测试目的**：验证连接成功与状态迁移序列。
- **流程与逻辑**：注册 `onStateChanged`，`requireSensor` 后 `connect()`，记录每次状态变化。
- **预期结果**：返回 True，按 `Disconnected→Connecting→Connected→Ready` 迁移，最终 `deviceState==Ready`、`isReady==True`。
- **有效性说明**：连接是后续操作前提；迁移顺序错误会导致指令在错误状态下发送。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### DEV-SM-002 connect 失败返回 False 并回调原因
- **优先级**：P0
- **测试目的**：验证连接失败路径。
- **流程与逻辑**：对已关机/超出范围的设备 `connect()`，观察返回与 `onErrorCallback`。
- **预期结果**：返回 False，`onErrorCallback` 收到原因。
- **有效性说明**：失败可观测、不静默。
- **可自动化**：semi-auto
- **人工介入**：人工关闭设备电源或移出范围。
- **测试结果**：待测试

### DEV-SM-003 非目标前缀设备不误连接
- **优先级**：P1
- **测试目的**：验证不误连其它设备。
- **流程与逻辑**：扫描到非 Cerelax 前缀设备，`requireSensor` 后确认不会自动连接目标 Cerelax。
- **预期结果**：只连接显式指定的设备。
- **有效性说明**：防止串设备。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### DEV-SM-004 disconnect 成功，Ready→Disconnecting→Disconnected
- **优先级**：P0
- **测试目的**：验证主动断开与状态迁移。
- **流程与逻辑**：Ready 后 `disconnect()`，记录状态迁移与返回。
- **预期结果**：返回 True，最终 `deviceState==Disconnected`，`onStateChanged` 触发。
- **有效性说明**：断开干净、状态正确。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### DEV-SM-005 起流中 disconnect 自动先停流
- **优先级**：P0
- **测试目的**：验证断开前自动停流。
- **流程与逻辑**：起流后直接 `disconnect()`，观察是否先停流再断开。
- **预期结果**：无异常，`isDataTransfering` 最终 False，连接断开。
- **有效性说明**：避免传输中硬断开导致资源问题。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### DEV-SM-006 Ready 前不发送命令
- **优先级**：P0
- **测试目的**：验证命令时序约束。
- **流程与逻辑**：在 `Connecting`/`Connected` 阶段调 `init`/`setParam`/`startDataNotification`，观察返回。
- **预期结果**：返回 False/Error，不崩溃。
- **有效性说明**：确认 SDK 内部状态门控。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### DEV-SM-007 BLEDevice 的 Name/Address/RSSI 与扫描一致
- **优先级**：P1
- **测试目的**：验证设备元数据一致性。
- **流程与逻辑**：比对 `sensor.BLEDevice` 与扫描结果的 Name/Address/RSSI。
- **预期结果**：一致。
- **有效性说明**：身份识别可靠，多设备不混淆。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### DEV-SM-008 autoReconnect=True（默认）异常断链自动恢复
- **优先级**：P0
- **测试目的**：验证自动重连恢复链路。
- **流程与逻辑**：连接起流后断电/超范围再恢复，观察是否 `connect→init→恢复 setParam→startDataNotification`。
- **预期结果**：恢复后重新 Ready 并恢复起流。
- **有效性说明**：异常恢复核心，长稳/移动场景关键。
- **可自动化**：semi-auto
- **人工介入**：人工断电/移出范围后恢复，观察恢复过程。
- **测试结果**：待测试

### DEV-SM-009 autoReconnect=False 不自动重连
- **优先级**：P1
- **测试目的**：验证关闭自动重连。
- **流程与逻辑**：设 `autoReconnect=False`，断链后观察是否重连。
- **预期结果**：不自动重连，仅触发 `onStateChanged(Disconnected)`/`onErrorCallback`。
- **有效性说明**：开关生效。
- **可自动化**：semi-auto
- **人工介入**：人工断电/移出范围。
- **测试结果**：待测试

### DEV-SM-010 onAutoReconnect(restore=True) 自定义恢复
- **优先级**：P1
- **测试目的**：验证自定义恢复回调。
- **流程与逻辑**：注册 `onAutoReconnect` 回调（签名 `(sensor, restore, answer)`），断链恢复后调用 `answer(True)` 表示由应用接管恢复流程，确认 SDK 跳过默认恢复。
- **预期结果**：`answer(True)` 生效，自定义路径接管，SDK 不执行默认恢复。
- **有效性说明**：恢复逻辑可定制。
- **可自动化**：semi-auto
- **人工介入**：人工断电/恢复。
- **测试结果**：待测试

### DEV-SM-011 多台设备同时 connect 互不干扰
- **优先级**：P0
- **测试目的**：验证并发连接（需 ≥2 台）。
- **流程与逻辑**：同时连接 2 台，观察状态/数据互不干扰。
- **预期结果**：两台各自 Ready，无串扰。
- **有效性说明**：并发安全性。
- **可自动化**：auto
- **人工介入**：无（需 ≥2 台设备）
- **测试结果**：待测试

### DEV-SM-012 突然断电/超范围触发 Disconnected 与错误回调
- **优先级**：P0
- **测试目的**：验证异常断链可观测。
- **流程与逻辑**：连接起流后突然断电/移出范围，记录 `onStateChanged`/`onErrorCallback`。
- **预期结果**：触发 `Disconnected` 与错误回调。
- **有效性说明**：异常可观测，为恢复/告警提供依据。
- **可自动化**：semi-auto
- **人工介入**：人工断电/移出范围。
- **测试结果**：待测试

### DEV-SM-013 isReady 与 deviceState==Ready 一致
- **优先级**：P1
- **测试目的**：验证 `isReady` 属性。
- **流程与逻辑**：在 Ready 与断开两种状态读取 `isReady` 与 `deviceState`。
- **预期结果**：Ready 时 `isReady==True`；断开后 False。
- **有效性说明**：快速判定接口正确。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### DEV-SM-014 onDeviceInfoUpdate 触发与就地更新
- **优先级**：P1
- **测试目的**：验证设备信息变更回调。
- **流程与逻辑**：注册 `onDeviceInfoUpdate`，连接 init 后（或采样率/链路参数变更时）观察回调与返回的 `DeviceInfo`。
- **预期结果**：链路参数/采样率变更时触发，返回对象已更新。
- **有效性说明**：设备能力动态感知。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### DEV-SM-015 connect 重复调用幂等
- **优先级**：P1
- **测试目的**：验证已连接/连接中再次 connect 无副作用。
- **流程与逻辑**：Ready 后再次 `connect()`；`Connecting` 阶段再次 `connect()`。
- **预期结果**：不崩溃不抛异常，不产生重复连接；状态不倒退（返回 False 或幂等 True 均记录，以 README/实测为准）。
- **有效性说明**：防止重复连接导致资源/句柄重复占用。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### DEV-SM-016 disconnect 重复调用幂等
- **优先级**：P1
- **测试目的**：验证已断开/断开中再次 disconnect 无副作用。
- **流程与逻辑**：`Disconnected` 后再次 `disconnect()`；断开过程中再次 `disconnect()`。
- **预期结果**：不崩溃不抛异常，最终 `deviceState==Disconnected`。
- **有效性说明**：断开幂等，防止重复断开报错。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### DEV-SM-017 BLE MTU 协商前后数据分包正确
- **优先级**：P1
- **测试目的**：验证 MTU 协商对数据分包的影响（§七 BLE 专项）。
- **流程与逻辑**：连接 init 起流后，从 profile log 或 bin 观察协商前后的 MTU 值；起流大数据包（如多通道 EEG）验证分包正确、无丢帧、`getLostPackageCount` 不异常增长。
- **预期结果**：协商前后 MTU 值被记录；大数据包按协商后 MTU 正确分包，采样率/丢包率正常（允许容差，来源：实测标定）。
- **有效性说明**：MTU 过小会导致分包异常/丢包，是 BLE 高频问题点。
- **可自动化**：semi-auto
- **人工介入**：无（MTU 由链路自动协商，仅需观察 log/bin）
- **测试结果**：待测试

### DEV-SM-018 配对/bond 信息持久与免配对重连
- **优先级**：P1
- **测试目的**：验证已配对设备重连是否免配对、bond 信息是否持久（§七 BLE 专项）。
- **流程与逻辑**：首次连接完成配对/bond → 断开 → 再次 `connect()`，观察是否免配对直接重连成功；重启 SDK 进程后重连，确认 bond 信息持久。
- **预期结果**：已配对设备再次连接免配对成功；bond 信息在重启后仍持久（若 SDK 不管理 bond，则记录"SDK 不持久化 bond，由 OS 管理"，不判失败）。
- **有效性说明**：bond 丢失会导致每次重连重新配对，影响体验与稳定性。
- **可自动化**：semi-auto
- **人工介入**：首次配对时可能需在【电脑】确认配对弹窗。
- **测试结果**：待测试

### DEV-SM-019 广播间隔与重连退避符合预期
- **优先级**：P1
- **测试目的**：验证扫描时序与重连退避是否符合预期（§七 BLE 专项）。
- **流程与逻辑**：记录目标设备广播间隔（连续扫描统计广播包时间间隔）；异常断链后观察 SDK 自动重连的重试间隔是否按退避策略递增，且最终恢复。
- **预期结果**：广播间隔稳定在合理范围；重连退避间隔递增、最终恢复连接（具体退避数值以 SDK 文档/README 为准，未文档化则记录实测值）。
- **有效性说明**：广播间隔影响扫描发现成功率；退避策略避免重连风暴。
- **可自动化**：semi-auto
- **人工介入**：人工制造断链（断电/移出范围）观察退避。
- **测试结果**：待测试
