# Cerelax 参数用例（PARAM-*）

> Cerelax 为 **2 通道脑电头环**，核心参数为 `EEG_SAMPLE_RATE`（唯一合法值 `250`）。
> **重要**：SDK 的 `setParam` 仅提供 `EEG_SAMPLE_RATE` 一个采样率设置接口；IMU（`50|100|200`）、PPG（`50|100|200|400`）采样率**无设置接口**，仅可读 `getDeviceInfo()` 的只读字段（`ImuSampleRate`/`PpgSampleRate`）。
> `NTF_*` 流开关、`FILTER_*` 滤波开关等按 `getDeviceInfo()` 运行时判定能力，不做硬编码假设。

### PARAM-FUNC-001 每个 NTF_* 键 ON/OFF 返回 OK，getParam 一致
- **测试目的**：验证数据流开关。
- **流程与逻辑**：对适用键（`NTF_EEG` 为核心；`NTF_PPG`/`NTF_IMU`（或四路 `NTF_GFORCE_*`）/`NTF_IMPEDANCE` 为重点；`NTF_SPO2`/`NTF_ACC`/`NTF_GYRO`/`NTF_EULER_DATA`/`NTF_QUATERNION`/`NTF_MAG_ANGLE_DATA` 按 `getDeviceInfo()` 对应 `ChannelCount` 判定，>0 测，==0 记录"不适用，跳过"；头环无 ECG/EMG/BRTH，不测）逐一 `setParam("ON"/"OFF")`，`getParam("NTF")` 核对。
- **预期结果**：返回 `OK`，`getParam` 状态一致。
- **有效性说明**：参数读写一致。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-FUNC-002 FILTER_* 键 ON/OFF
- **测试目的**：验证滤波开关（EEG 的 50/60Hz 工频陷波、高通/低通）。
- **流程与逻辑**：`setParam("FILTER_50HZ"/"FILTER_60HZ"/"FILTER_HPF"/"FILTER_LPF","ON"/"OFF")`，`getParam("FILTER")` 核对；设备不支持的键返回 `Error` 则记录"不支持"。
- **预期结果**：返回 `OK`，状态一致。
- **有效性说明**：滤波参数生效。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-FUNC-003 NTF_IMU 联动四个 NTF_GFORCE_*（待确认）
- **测试目的**：验证 IMU 主开关联动（Cerelax 的 IMU 是聚合流还是四路独立流待运行时确认）。
- **流程与逻辑**：若 `ImuChannelCount>0`（聚合流），`setParam("NTF_IMU","ON")` 应联动四个 `NTF_GFORCE_*`；若 `ImuChannelCount==0`（四路独立流），直接测四个 `NTF_GFORCE_*` 独立开关。
- **预期结果**：聚合流联动一致；四路独立流各自开关独立。
- **有效性说明**：设备能力边界。
- **可自动化**：auto（脚本保留能力门控）
- **人工介入**：无
- **测试结果**：待测试

### PARAM-FUNC-004 EEG_SAMPLE_RATE 设置（含逆向：500 非法）
- **测试目的**：验证 EEG 采样率设置（Cerelax 核心功能），含非法值逆向。
- **流程与逻辑**：从 `getParam("EEG_SAMPLE_RATE_LIST")` 获取可选值列表（预期仅 `250`），`setParam("EEG_SAMPLE_RATE", "250")` 后 `getParam("EEG_SAMPLE_RATE")` 核对；另测逆向值 `setParam("EEG_SAMPLE_RATE", "500")` 期待返回 `Error`（`500` 为非法值）。Cerelax 无 ECG，仅 EEG 单模态。
- **预期结果**：合法值 `250` 设置返回 `OK`、`getParam` 一致；非法值 `500` 返回 `Error` 且不改变当前采样率。
- **有效性说明**：EEG 采样率是 Cerelax 核心参数（仅支持 `250`）；`500` 非法值逆向验证防止"越界值被误接受"。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-FUNC-005 EEG_SAMPLE_RATE 不支持值返回 Error
- **测试目的**：验证非法采样率。
- **流程与逻辑**：`setParam("EEG_SAMPLE_RATE", 列表外值)` 与 `setParam("EEG_SAMPLE_RATE", 空串)`。
- **预期结果**：列表外值返回 `Error: unsupported sample rate ...`；空串被拒绝返回 `Error`。
- **有效性说明**：非法输入可观测。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-FUNC-006 getParam EEG_SAMPLE_RATE/LIST
- **测试目的**：验证采样率查询。
- **流程与逻辑**：`getParam("EEG_SAMPLE_RATE")` 与 `getParam("EEG_SAMPLE_RATE_LIST")`。
- **预期结果**：分别返回当前采样率与可选值列表（如 `"250"`，仅一个值）。
- **有效性说明**：查询接口正确。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-FUNC-007 起流中修改参数自动停流重起并生效
- **测试目的**：验证起流中改参。
- **流程与逻辑**：起流中改 `NTF_*`/`FILTER_*`/`EEG_SAMPLE_RATE`，观察是否自动停流重起且新状态生效。
- **预期结果**：自动重起，新参数生效。
- **有效性说明**：动态配置能力。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-FUNC-008 未知 key 返回 Error 开头
- **测试目的**：验证非法 key。
- **流程与逻辑**：`setParam("UNKNOWN_KEY","ON")`。
- **预期结果**：返回以 `Error` 开头。
- **有效性说明**：非法输入可观测。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-FUNC-009 NEUCIR_*（不适用）
- **测试目的**：NeuCir 远程控制参数。
- **流程与逻辑**：—（Cerelax 非 NeuCir 设备）。
- **预期结果**：记录"不适用"。
- **有效性说明**：设备能力边界。
- **可自动化**：manual
- **人工介入**：不执行，仅记录。
- **测试结果**：不适用

### PARAM-FUNC-010 DEBUG_BLE_DATA_PATH / DEBUG_LOG_PATH
- **测试目的**：验证调试路径参数。
- **流程与逻辑**：`setParam("DEBUG_BLE_DATA_PATH", True)`、`setParam("DEBUG_LOG_PATH", ...)`，读回，关闭。
- **预期结果**：设置/读取/关闭正常。
- **有效性说明**：bin 导出与日志开关。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-FUNC-011 IMU/PPG 采样率只读字段合法（SDK 无设置接口）
- **测试目的**：验证 IMU/PPG 采样率能力字段（因 SDK 无 `IMU_SAMPLE_RATE`/`PPG_SAMPLE_RATE` 设置接口，仅作只读验证）。
- **流程与逻辑**：Ready + init 后 `getDeviceInfo()`，读 `ImuSampleRate`/`PpgSampleRate`；起流后用实测数据率与只读标称值比对（IMU 标称 `50|100|200` 之一，PPG 标称 `50|100|200|400` 之一）。
- **预期结果**：只读字段为合法正值（`>0`），实测数据率与之相符（允许容差）。
- **有效性说明**：确认 SDK 未提供设置接口时的能力可观测性；「设置 + 生效」验证见 PARAM-FUNC-012/013（待 SDK 实现后执行）。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-FUNC-012 IMU_SAMPLE_RATE 设置（当前版本未实现，规划中）
- **测试目的**：验证 IMU 采样率设置（未来功能；当前 SDK 版本未提供设置接口，用例先行登记）。
- **流程与逻辑**：`getParam("IMU_SAMPLE_RATE_LIST")` 获取可选值列表（预期 `50|100|200`），逐一 `setParam("IMU_SAMPLE_RATE", value)`，`getParam("IMU_SAMPLE_RATE")` 核对；另测列表外值（如 `400`）与空串。接口 key 以 SDK 最终实现为准（对齐 `EEG_SAMPLE_RATE` 命名）。
- **预期结果**：SDK 实现后——设置返回 `OK`、`getParam` 一致，列表外值/空串返回 `Error`；当前版本——`setParam` 返回 `Error`（接口不存在），记为「待实现」。
- **有效性说明**：IMU 采样率可配置为需求项，先登记避免遗漏。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待实现（当前 SDK 无 `IMU_SAMPLE_RATE` 设置接口）

### PARAM-FUNC-013 PPG_SAMPLE_RATE 设置（当前版本未实现，规划中）
- **测试目的**：验证 PPG 采样率设置（未来功能；当前 SDK 版本未提供设置接口，用例先行登记）。
- **流程与逻辑**：`getParam("PPG_SAMPLE_RATE_LIST")` 获取可选值列表（预期 `50|100|200|400`），逐一 `setParam("PPG_SAMPLE_RATE", value)`，`getParam("PPG_SAMPLE_RATE")` 核对；另测列表外值（如 `1000`）与空串。接口 key 以 SDK 最终实现为准（对齐 `EEG_SAMPLE_RATE` 命名）。
- **预期结果**：SDK 实现后——设置返回 `OK`、`getParam` 一致，列表外值/空串返回 `Error`；当前版本——`setParam` 返回 `Error`（接口不存在），记为「待实现」。
- **有效性说明**：PPG 采样率可配置为需求项，先登记避免遗漏。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待实现（当前 SDK 无 `PPG_SAMPLE_RATE` 设置接口）

### PARAM-BND-001 setParam 非法 value
- **测试目的**：验证非法值处理。
- **流程与逻辑**：对代表性 `NTF_*` 与 `FILTER_*` 键（如 `NTF_EEG`/`NTF_PPG`/`NTF_IMPEDANCE`/`FILTER_50HZ`/`FILTER_LPF`，按设备实际能力选取）逐一传空串、`ON/OFF` 以外、大小写、`None`。
- **预期结果**：非法值被拒绝/返回 Error，不崩溃。
- **有效性说明**：输入校验。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-BND-002 setParam/getParam 空串/None key
- **测试目的**：验证空/None key 的逆向处理。
- **流程与逻辑**：`setParam("", "ON")`、`setParam(None, "ON")`、`getParam("")`、`getParam(None)`。
- **预期结果**：返回以 `Error` 开头或明确拒绝，不崩溃、不抛异常。
- **有效性说明**：空 key 不误命中、不崩溃。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试

### PARAM-ROB-001 同一 key 重复 setParam 幂等
- **测试目的**：验证重复设参幂等。
- **流程与逻辑**：对同一 key（如 `NTF_EEG`）连续两次 `setParam("ON")`，`getParam` 核对；起流中重复设同一值观察是否无谓停流重起。
- **预期结果**：两次均返回 `OK`，状态一致；起流中重复设相同值不产生额外停流重起（或行为一致，以 README 为准）。
- **有效性说明**：重复设参无副作用。
- **可自动化**：auto
- **人工介入**：无
- **测试结果**：待测试
